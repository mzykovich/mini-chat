# Corp Chat Template — передача фронтенд-разработчику

Этот проект — корпоративный чат (аналог Discord/Slack) для **внутреннего использования в компании**.
Фронтенд — статический, отдаётся сервером Node.js из папки `/public`.

---

## 📁 Структура

- `public/index.html`
  - HTML-разметка
  - ⚠️ ID и data-атрибуты менять аккуратно — они используются в JS

- `public/styles.css`
  - Весь дизайн и стили
  - Можно полностью перерабатывать под фирменный стиль компании

- `public/app.js`
  - Вся клиентская логика:
    - авторизация
    - WebSocket
    - рендер сообщений, каналов, DM
    - прочтения, онлайн, редактирование, удаление

---

## ▶️ Как запустить
Фронт автоматически работает вместе с сервером:

```bash
npm install
npm start
```

Открыть:
```
http://localhost:PORT
```

---

## 🧩 Основные зоны интерфейса
- Левая панель:
  - Авторизация
  - Каналы
  - Личные сообщения (DM)
  - Сотрудники
  - Админка

- Центральная часть:
  - История сообщений
  - Статусы онлайн
  - Прочитано ✓ / ✓✓
  - Редактирование / удаление сообщений
  - Прикрепление файлов

---

## 🌐 REST API (используется фронтом)

### Авторизация
- `POST /api/register`
- `POST /api/login`
- `POST /api/logout`
- `GET /api/me`

### Данные
- `GET /api/positions`
- `GET /api/channels`
- `GET /api/users`
- `GET /api/dm`
- `POST /api/dm/open`

### Админка
- `GET /api/admin/users`
- `GET /api/admin/roles`
- `POST /api/admin/roles`
- `GET /api/admin/channels`
- `POST /api/admin/channels`
- `GET /api/admin/channels/:id/access`
- `POST /api/admin/channels/:id/access`
- `GET /api/audit`

### DM аудит (OWNER)
- `GET /api/admin/dm/chats`
- `GET /api/admin/dm/chats/:id/messages`

---

## 🔌 WebSocket события

### Клиент → Сервер
- `join`, `chat`, `read`
- `edit`, `delete`
- `dm_join`, `dm_chat`, `dm_read`
- `dm_edit`, `dm_delete`

### Сервер → Клиент
- `init`
- `history`, `chat`
- `message_edited`, `message_deleted`
- `presence_update`
- `read_progress_batch`
- `dm_history`, `dm_chat`
- `dm_message_edited`, `dm_message_deleted`
- `refresh_lists`


## ❗ Важно
- Не удалять ID элементов без проверки
- WebSocket протокол считается частью API
- Все изменения дизайна безопаснее делать через `styles.css`
